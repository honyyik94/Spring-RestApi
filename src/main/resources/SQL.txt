CREATE TABLE TRANSACTIONS (
ID CHAR(36) NOT NULL PRIMARY KEY,
ACCOUNT_NUMBER BIGINT,
TRX_AMOUNT DECIMAL(30, 2),
DESCRIPTION VARCHAR(255),
TRX_DATE DATE,
TRX_TIME TIME,
CUSTOMER_ID BIGINT,
version INT DEFAULT 0
);